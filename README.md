# smart_chrome_tab_suspender

A lightweight Google Chrome extension designed to suspend inactive tabs to save memory and CPU usage.

## Features
- Automatically detects inactive tabs after a set period.
- Frees up system memory (RAM) and CPU resources.
- Restores tabs instantly upon focus.
